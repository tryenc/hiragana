`HiraganaChart` in `landscape-primary` orientation
```jsx
<HiraganaChart orientation="landscape-primary" />
```

`HiraganaChart` in `portrait-primary` orientation
```jsx
<HiraganaChart orientation="portrait-primary" />
```