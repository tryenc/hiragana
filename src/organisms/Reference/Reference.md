`Reference` in `landscape-primary` orientation
```jsx
<Reference orientation="landscape-primary" />
```

`Reference` in `portrait-primary` orientation
```jsx
<Reference orientation="portrait-primary" />
```